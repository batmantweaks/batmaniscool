# batmaniscool 4.7.2

- Adds read-only checks before administrator-only, TCP Fast Open, and advanced network-adapter controls run.
- Stops unsupported controls before they can create an execution error.
- Shows a direct Run as administrator instruction when Windows requires it.
