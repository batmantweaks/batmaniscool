# batmaniscool 4.7.1

- Cleans up compatibility errors so PowerShell/XML output is never shown in the app.
- Uses a per-user Widgets button setting instead of an administrator-only policy.
- Updates network-adapter controls for better compatibility.
- Keeps hardware- and Windows-version-specific advanced controls out of the default tuning profiles. They remain available as individual controls for compatible PCs.
