# batmaniscool 4.7.3

- The installed app now requests Windows administrator approval before it opens.
- This allows supported system-level tweaks to run with the required permission.
- Hardware- or Windows-version-specific controls still run a compatibility check before making a change.
